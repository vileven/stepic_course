server {
	listen 80;
	access_log /var/log/nginx.acces_log simle;
	error_log /var/log/nginx/error_log info;

	location / {
		return 404;	
	}

	location ~* \.(jpg|jpeg|gif|png|ico|css|zip|rar|pdf)$ {
		root /home/box/web/public;
	}
	
	location ^~ /uploads/ {
		root /home/box/web;
	}
}

